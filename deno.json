{
  "//": "Deno must NOT adopt the npm project rooted here. package.json exists only for jsdom, a devDependency of tools/test-composer.mjs. Without this, `deno check` switches to node_modules resolution and fails to resolve the edge functions' remote types entirely — which check-functions.mjs then read as 'no type errors'. The gate was silently dead from c47f05d until this file.",
  "nodeModulesDir": "none"
}
